import styled from 'styled-components';

export const DeveloperEditor = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 1rem;
`;

export const EditorContainer = styled.div`
    display: flex;
    flex-direction: column;
    gap: 1rem;

`;
